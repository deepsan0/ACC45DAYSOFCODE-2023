#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    int x,y,z,q;
	    scanf("%d %d %d",&x,&y,&z);
	    q=y/x;
	    if(q<=z)
	    {
	        
	        printf("%d\n",(z-q));
	        
	        
	    }
	    else
	    {
	        printf("0\n");
	    }
	}
	return 0;
}

