#include <stdio.h>
int main(void)
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    int a1,a2,b1,b2;
	    scanf("%d %d %d %d",&a1,&a2,&b1,&b2);
	    if( ((a1-a2) + (b1-b2)) < 0 )
	    {
	        printf("YES\n");
	    }
	    else
	    {
	        printf("NO\n");
	    }
	}
	return 0;
}
