#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    int x,n;
	    scanf("%d %d",&n,&x);
	    if((n-x)<x)
	    {
	        printf("%d\n",n-x);
	    }
	    else
	    {
	        printf("%d\n",x);
	    }
	}
	return 0;
}

