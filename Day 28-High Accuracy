#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    int x,w,i;
	    scanf("%d",&x);
	    if(x%3!=0)
	    {
	        w=x%3;
	        i=3-w;
	        printf("%d\n",i);
	    }
	    else
	    {
	        printf("0\n");
	    }
	}
	return 0;
}

